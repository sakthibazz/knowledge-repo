export class FileDetails {
    constructor(
        fileUrl: string,
        fileName: string,
        uploadFileTo: string,
        uploadBy: string,
        tag: string,
        docProject: string,
        docTeam: string
    ) { }
}